image: silex/emacs:latest

before_script:
  - apt-get update && apt-get install -y python python-pip
  - pip install awscli

publish:
  script:
    - which emacs
    - $CI_PROJECT_DIR/build.el
    - aws s3 sync $HOME/.www.alexrecker.com s3://$S3_BUCKET --delete
